project:
  # Whether to enable telemetry (default: true). No personal data is collected.
  enable_telemetry: false

  # List of environment variables to be provided by each user to use the app.
  user_env: []

  # Duration (in seconds) during which the session is saved when the connection is lost
  session_timeout: 3600

  # Enable third parties caching (e.g LangChain cache)
  cache: false

  # Authorized origins
  allow_origins: ["*"]

  # Follow symlink for asset mount (see https://github.com/Chainlit/chainlit/issues/317)
  # follow_symlink: false

features:
  # Show the prompt playground
  prompt_playground: true

  # Process and display HTML in messages. This can be a security risk (see https://stackoverflow.com/questions/19603097/why-is-it-dangerous-to-render-user-generated-html-or-javascript)
  unsafe_allow_html: false

  # Process and display mathematical expressions. This can conflict with "$" characters in messages.
  latex: false

  # Authorize users to upload files with messages
  multi_modal: true

  # Allows user to use speech to text
  speech_to_text:
    enabled: false
    # See all languages here https://github.com/JamesBrill/react-speech-kit#language-support
    # language: en-US

ui:
  # Name of the app and chatbot.
  name: "GENeticAI - Azure AI Agent"

  # Show the readme while the thread is empty.
  show_readme_as_default: true

  # Description of the app and chatbot. This is used for HTML tags.
  description: "AI-powered chat interface connecting to Azure AI Foundry agents"

  # Large size content are by default collapsed for a cleaner ui
  default_collapse_content: true

  # The default value for the expand messages settings.
  default_expand_messages: false

  # Hide the chain of thought details from the user in the UI.
  hide_cot: false

  # Link to your github repo. This will add a github button in the UI's header.
  # github: https://github.com/yourusername/genetic-ai-chat

  # Specify a CSS file that can be used to customize the user interface.
  # The CSS file can be served from the public directory or via an external link.
  # custom_css: "/public/test.css"

  # Specify a Javascript file that can be used to customize the user interface.
  # The Javascript file can be served from the public directory.
  # custom_js: "/public/test.js"

  # Specify a custom font url.
  # custom_font: "https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap"

  # Specify a custom build directory for the UI. This is used to customize the UI using React.
  # custom_build: "./public/build"

meta:
  generated_by: "1.0.0"